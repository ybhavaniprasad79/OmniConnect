MONGO_URI=mongodb://127.0.0.1:27017/omnicconnect
JWT_SECRET=change_this_secret
PORT=4000

# Optional SMTP settings for email fallback (used by nodemailer integration)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# Providers credentials (placeholders) - replace with real provider keys when integrating
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+1234567890
